---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';

const currentPath = Astro.url.pathname;
---

<Layout 
  title="Empresa | Austech - Austral Technology" 
  description="Experiencia e innovación en la fabricación de herramientas"
>
  <Navbar activeItem={currentPath} />
  
  <main class="overflow-hidden">
    <!-- Hero Section con Parallax -->
    <div class="relative h-screen flex items-center justify-center">
      <div class="absolute inset-0 parallax-bg">
        <img 
          src="/images/empresa.jpg" 
          alt="Instalaciones Austech" 
          class="absolute inset-0 w-full h-full object-cover"
        />
        <div class="absolute inset-0 bg-gradient-to-b from-black/70 via-black/50 to-transparent"></div>
      </div>
      <div class="relative z-10 text-center px-4 animate-fade-in">
        <h1 class="text-7xl font-bold text-white mb-6 tracking-tight">EMPRESA</h1>
        <p class="text-xl text-gray-200 max-w-2xl mx-auto leading-relaxed">
          Innovación y excelencia en la fabricación de herramientas de precisión
        </p>
        <div class="mt-8">
          <a href="#content" class="inline-block animate-bounce">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>

    <!-- Contenido Principal -->
    <div id="content" class="bg-white">
      <!-- Experiencia Section -->
      <section class="py-24 relative overflow-hidden">
        <div class="container mx-auto px-4">
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
            <div class="relative">
              <div class="aspect-square rounded-full overflow-hidden border-8 border-primary/10 transform hover:rotate-3 transition-transform duration-500">
                <img 
                  src="/images/empresa.jpg" 
                  alt="Maquinaria Austech" 
                  class="w-full h-full object-cover"
                />
              </div>
              <div class="absolute -bottom-8 -right-8 w-48 h-48 bg-primary/10 rounded-full -z-10"></div>
            </div>
            <div class="space-y-6">
              <h2 class="text-4xl font-bold text-gray-900 leading-tight">
                Experiencia e <span class="text-primary">innovación</span>
              </h2>
              <p class="text-gray-700 text-lg leading-relaxed">
                Somos una empresa joven, en la cual se juntan experiencias distintas que han estado trabajando en la 
                fabricación de herramientas durante décadas. Aceptamos el desafío de crear un nuevo 
                emprendimiento para responder a los desafíos de un mercado que tiene prácticamente encima la 4ª 
                Revolución Industrial.
              </p>
              <p class="text-xl font-semibold text-primary">
                Nosotros queremos ser parte de esta revolución.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- A hombros de gigantes - Con diseño en capas -->
      <section class="py-24 bg-gray-50 relative">
        <div class="absolute inset-0 bg-gradient-to-br from-primary/5 to-transparent"></div>
        <div class="container mx-auto px-4 relative">
          <div class="max-w-4xl mx-auto text-center mb-16">
            <h2 class="text-4xl font-bold text-gray-900 mb-6">A hombros de gigantes</h2>
            <p class="text-lg text-gray-700 leading-relaxed">
              Newton escribió: "Si he visto más lejos es porque estoy sentado sobre los hombros de gigantes".
            </p>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {['CMT', 'Stark', 'Saidtools'].map((partner) => (
              <div class="group">
                <div class="bg-white rounded-xl shadow-xl p-8 transform hover:-translate-y-2 transition-all duration-300">
                  <div class="aspect-video mb-6 overflow-hidden rounded-lg">
                    <img 
                      src="/images/empresa.jpg" 
                      alt={`Partner ${partner}`}
                      class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-500"
                    />
                  </div>
                  <h3 class="text-xl font-bold text-gray-900 mb-3">{partner}</h3>
                  <p class="text-gray-600">
                    Aliados estratégicos con los que crecemos juntos, compartiendo tecnología y conocimiento.
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- Última tecnología - Con diseño moderno -->
      <section class="py-24 relative overflow-hidden">
        <div class="container mx-auto px-4">
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
            <div class="order-2 lg:order-1 space-y-6">
              <h2 class="text-4xl font-bold text-gray-900">
                La última <span class="text-primary">tecnología</span>
              </h2>
              <p class="text-gray-700 text-lg leading-relaxed">
                Contamos con la mejor tecnología para la fabricación y el servicio de herramientas. Nuestras 
                máquinas Walter, Vollmer, Haimer, Deckel Maho DMU 50, Doosan Puma 3100 Y, entre otras son 
                simplemente lo mejor que ofrece el mercado.
              </p>
              <div class="flex flex-wrap gap-4">
                {['Walter', 'Vollmer', 'Haimer'].map((marca) => (
                  <span class="px-4 py-2 bg-gray-100 rounded-full text-gray-700 text-sm font-medium">
                    {marca}
                  </span>
                ))}
              </div>
            </div>
            <div class="order-1 lg:order-2 relative">
              <div class="aspect-[4/3] rounded-2xl overflow-hidden shadow-2xl transform hover:rotate-2 transition-transform duration-500">
                <img 
                  src="/images/empresa.jpg" 
                  alt="Tecnología Austech"
                  class="w-full h-full object-cover"
                />
              </div>
              <div class="absolute -top-8 -left-8 w-48 h-48 bg-primary/10 rounded-full -z-10"></div>
            </div>
          </div>
        </div>
      </section>

      <!-- Capacitación continua - Con diseño elegante -->
      <section class="py-24 bg-gray-900 text-white relative overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-br from-primary/20 to-transparent"></div>
        <div class="container mx-auto px-4 relative">
          <div class="max-w-4xl mx-auto text-center">
            <h2 class="text-4xl font-bold mb-8">Capacitación continua</h2>
            <p class="text-xl text-gray-300 leading-relaxed mb-12">
              Todo esto no sería suficiente sin poder contar con un personal altamente capacitado y siempre al día 
              con la innovación. Enviamos regularmente a Europa a nuestros técnicos para que se formen en las 
              plantas súper tecnológicas de nuestros partners.
            </p>
            <p class="text-2xl font-bold text-primary">
              Porque nosotros creemos en las personas.
            </p>
          </div>
        </div>
      </section>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-[#111827] text-white border-t border-gray-800 py-12">
    <div class="container mx-auto px-4">
      <div class="text-center">
        <p class="text-gray-400">Copyright © {new Date().getFullYear()}. Todos los derechos reservados.</p>
      </div>
    </div>
  </footer>
</Layout>

<style>
  .parallax-bg {
    transform: translateZ(-1px) scale(2);
    z-index: -1;
  }

  .animate-fade-in {
    animation: fadeIn 1s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  html {
    scroll-behavior: smooth;
  }
</style> 